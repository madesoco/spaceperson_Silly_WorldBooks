<TimePlace> 
# 每次正文结束后，**紧跟着**进行一次时间地点栏的更新
格式如下：
<Tp>
``` 时间: ${某某}纪元${yyy}年-${mm}月-${dd}日-${hh}:${mm} ```
``` 地点: ${大陆方位}-${势力}-${子级势力}-${城市/地标}-${区位}-${详细位置} ```
</Tp>
要求:
  - 24小时制，实时更新
  - 地点按实际情况保留3~6层，从大到小排列
</TimePlace>{{// 命定时间地点显示绿灯关键词触发器}}